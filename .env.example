# Backend
PORT=3001
JWT_SECRET=change-me-in-production
FRONTEND_URL=http://localhost:5173
CHECK_INTERVAL_HOURS=6
ENABLE_PUPPETEER=false

# Email alerts (optional — logs to console if unset)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Dropwatch <alerts@localhost>

# Frontend (Vite)
VITE_API_URL=
VITE_API_PUBLIC_URL=http://localhost:3001
